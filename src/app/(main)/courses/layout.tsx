export const metadata = {
  title: "Courses",
  description: "Explore courses online",
};
export default function CoursesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
